// Default slide duration used when a content item has no duration_seconds set.
export const DEFAULT_SLIDE_DURATION_SECONDS = 15;

// Display settings defaults used in settings bootstrap.
export const DEFAULT_ROTATION_INTERVAL_SECONDS = 15;
